/**
 * Deck storage — Issue #10.
 *
 * Cards are stored in IndexedDB. This stub satisfies the import in SettingsView
 * until Issue #10 is implemented.
 */

export async function clearAllCards(): Promise<void> {
  // stub — implemented in Issue #10
}
